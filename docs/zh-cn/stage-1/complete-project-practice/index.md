---
title: '完整项目实战：从想法到作品'
description: '把已经接入 AI 的原型交给真实用户试用，根据观察完成一轮修改，并整理成可以运行、说明和展示的阶段作品。'
---

<script setup>
import { relatedArticlesMap } from '@theme/data/relatedArticles'
import ProductFinishMap from './ProductFinishMap.vue'

const duration = '约 <strong>2～3 天</strong>'
const relatedArticles =
  relatedArticlesMap['zh-cn/stage-1/complete-project-practice'] ?? []
</script>

# 完整项目实战：从想法到作品

<ProductJourney current="finish" />

## 章节导读

<ChapterIntroduction :duration="duration" :tags="['核心流程', '产品体验', '用户试用', '作品展示']" coreOutput="1 个别人可以独立完成核心任务的 AI 产品" expectedOutput="经过至少一轮真实试用和修改的 Web 作品">

前面的章节已经完成了三件重要的事：找到一个值得解决的问题，把方案做成可以操作的原型，再为其中的核心动作接入真实 AI 能力。

现在的产品可能已经“能跑”：你知道从哪里填写内容，也知道点哪个按钮可以得到结果。但如果把电脑交给一位第一次见到它的人，对方未必知道怎样开始；遇到等待或失败，也可能以为页面坏了。

所以，本章不再增加一批新技术。我们会选择一条最重要的用户任务，把它从头到尾走通；然后请真实用户独立试用，根据观察做一轮修改，最后整理成一份可以运行、说明和展示的阶段作品。

</ChapterIntroduction>

<div style="margin: 50px 0;">
  <ClientOnly>
    <StepBar :active="0" :items="[
      { title: '走通任务', description: '明确开始与完成' },
      { title: '补齐体验', description: '处理关键时刻' },
      { title: '真实试用', description: '观察而不是代操作' },
      { title: '整理作品', description: '说明过程与结果' }
    ]" />
  </ClientOnly>
</div>

<ProductFinishMap />

## 1. 先选一条必须走通的任务

“完成产品”不等于把想到的功能全部做完。第一阶段更重要的是：用户能否不依赖你的讲解，独立完成一件有价值的事。

仍以电商内容工作台为例，这件事可以是：

> 一位电商运营上传商品图片，补充必要信息，生成一版图文草稿，检查结果后复制或保存，用于后续修改和上架。

这句话给出了任务的开始和结束。上传图片之前的账号体系、团队权限，以及复制结果之后的正式发布，都可以留到以后；这一章先把中间这条路走顺。

### 1.1 把核心流程写成用户动作

不要先列页面和组件，而是按用户实际操作的顺序写：

1. 打开页面，知道这个工具可以帮自己做什么；
2. 上传一张商品图片，填写名称、材质等必要信息；
3. 点击“生成文案”，看见页面正在处理；
4. 检查 AI 返回的标题和卖点，发现问题时可以修改或重新生成；
5. 复制、下载或暂时保存结果，完成这次任务。

如果其中某一步只能靠你在旁边解释，流程就还没有真正完成。反过来，如果一个功能不影响这五步，比如团队成员管理或复杂的数据看板，可以先放进“以后再做”的清单。

::: tip 怎样判断范围是否合适
把任务交给一个第一次看到产品的人。如果你能用一句话说清目标，并让他在几分钟内尝试完成，范围通常就比较合适。
:::

### 1.2 从空白状态重新走一遍

开发时，我们经常在已经填好数据、已经生成过结果的页面上继续修改，很容易忽略第一次使用的感受。测试时可以刷新数据，或者打开一个没有使用记录的新窗口，再从第一步开始。

走完整条流程时，把下面这些时刻记下来：

- 第一次打开时，不看说明能不能判断产品用途；
- 不填内容直接点击，页面会发生什么；
- 上传错误格式的图片，能不能知道怎样改；
- 等待 AI 返回时，会不会误以为没有点击成功；
- 得到结果以后，下一步操作是否明显；
- 刷新页面或请求失败后，刚才的输入是否还在。

可以请 AI IDE 帮你检查代码，但不要让它替代实际操作：

```text
请先不要修改代码。

按照下面这条用户任务检查当前项目：
用户上传商品图片，填写必要信息，生成文案，
检查结果后复制或保存。

请告诉我这条流程涉及哪些页面和文件，
并列出目前可能中断流程的地方。
```

AI IDE 给出的只是检查线索。最后仍要亲自点击页面，确认问题是否真的存在。

## 2. 补齐用户会经历的四个时刻

一条核心流程通常包含四个时刻：开始操作、等待结果、查看结果，以及处理失败。我们不需要为每种状态做复杂设计，但要让用户始终知道页面发生了什么、接下来可以做什么。

### 2.1 开始前：告诉用户怎样迈出第一步

空白页面不应只放一个输入框。可以准备一句简短说明、一组示例内容，或者在上传区域标明支持的图片格式和大小。

如果表单字段很多，先保留生成结果真正需要的部分。比如商品名称、图片和核心特点可能是必填项，品牌、参考链接和更多风格参数可以放到“更多设置”中。用户第一次使用时，不应该先完成一份很长的登记表。

### 2.2 等待时：保留上下文，并给出反馈

AI 请求往往需要几秒甚至更久。点击以后，按钮应该显示“生成中”并暂时避免重复提交；已经填写的内容不要突然消失，页面也不要立刻跳到一个空白结果区。

![商品内容生成过程中的等待状态](../building-prototype/images/index-2026-01-14-15-50-05.png)

*等待状态不需要复杂动画。让用户看见任务已经开始，并且原来的输入和页面位置仍然保留，就已经解决了大部分困惑。*

如果图片或视频需要排队，可以进一步显示“正在排队”“正在生成”等阶段。不要编造一个看似精确的百分比，除非接口真的提供了进度。

### 2.3 得到结果后：让用户继续完成工作

AI 返回内容不是流程的终点。用户通常还要检查事实、修改措辞，并把结果带到下一步。因此结果区至少应提供一种可继续操作的方式，例如编辑、复制、下载或重新生成。

![图片理解和文案生成接入后的结果页面](../integrating-ai-capabilities/images/index-2026-01-20-15-35-41.png)

*这个页面先保留用户上传的商品图片，再把识别和生成结果放在下方。用户可以对照原图检查内容，而不是只能接受模型的一次回答。*

如果结果中存在模型无法确认的信息，可以直接标出来，让用户补充或删除。比起把一段文字包装成“最终答案”，这种做法更符合真实工作过程。

### 2.4 失败时：说明发生了什么，也保留重试机会

网络中断、额度不足和文件格式错误都会导致请求失败。页面不必向普通用户展示整段技术报错，但要说明这次操作没有完成，并提供重试或返回修改的入口。

例如：

- 图片格式不支持：说明当前支持的格式，并允许重新选择；
- 必填信息缺失：在对应字段附近提示，不要只弹出一句“参数错误”；
- AI 服务暂时不可用：保留已经填写的内容，并提供“重新生成”；
- 生成结果不符合预期：允许修改输入后再试，而不是让用户从头开始。

如果刷新页面会让用户丢掉一大段输入，可以使用 LocalStorage 暂时保存表单草稿。这里只保存继续任务所需的普通数据，不要把 API Key、真实客户资料或敏感文件写进浏览器存储。

可以把本节要求一次交给 AI IDE：

```text
请围绕“上传商品图片并生成文案”这条流程，
检查开始、等待、成功和失败四个时刻。

优先修复会阻止用户继续操作的问题：
- 必填项缺少提示；
- 请求期间可以重复点击；
- 失败后输入被清空；
- 结果没有编辑、复制或重新生成入口。

修改前先告诉我准备改哪些文件，
完成后给出一套手动测试步骤。
```

## 3. 把产品交给一个真实用户

自己测试时，我们知道每个按钮为什么存在，也会下意识避开还没做好的地方。要发现真正的问题，需要让别人独立完成任务。

优先邀请接近目标用户的人。电商内容工具可以找做过运营、开过网店或经常制作商品内容的人；如果暂时找不到，至少选择一位没有参与开发、也不了解页面结构的人。

### 3.1 给任务，不要教操作

测试开始时，只说明目标：

> 请用这个工具，根据这张商品图片生成一版标题和卖点。检查内容以后，把你认为可以继续修改的版本复制出来。

接下来先观察，不要马上告诉对方该点击哪里。记录他停顿、返回、重复点击和向你提问的位置。如果你一开口就解释，原本应该由页面解决的问题就会被掩盖。

三位用户通常已经能暴露不少明显问题。此时不必追求正式的研究报告，可以使用一张简单记录表：

| 观察到的行为 | 可能说明什么 | 下一步先验证什么 |
| --- | --- | --- |
| 打开页面后停了很久 | 不知道产品用途或从哪里开始 | 增加一句用途说明和示例输入 |
| 连续点击三次生成按钮 | 没看见请求已经开始 | 禁用按钮，并显示清楚的等待状态 |
| 生成后询问“然后呢” | 结果区缺少下一步动作 | 增加编辑、复制或重新生成入口 |
| 出错后重新填写全部内容 | 失败时没有保留输入 | 保留表单内容，并提供重试 |

### 3.2 结束后再问三个问题

用户完成或放弃任务以后，再问：

1. 刚才哪一步最不确定？
2. 生成结果中，哪些内容你会直接使用，哪些一定要修改？
3. 如果下次还要完成同样的工作，你愿不愿意再用一次？为什么？

不要只问“你觉得好不好用”。礼貌性的“挺好的”很难指导下一步修改，具体行为和具体例子更有价值。

::: warning 使用真实素材时
测试者上传的商品、录音或文档可能包含真实业务信息。开始前要说明内容会发送给哪类 AI 服务，避免使用没有授权的客户资料，并在测试后删除不再需要的文件。
:::

## 4. 根据观察完成一轮修改

一次试用往往会带来很多建议。不要全部接进第一版，否则产品会再次变大，也很难判断哪些修改真正有效。

可以按照下面的顺序处理：

1. **无法完成任务**：按钮无效、请求失败、结果无法带走；
2. **结果明显不可信**：编造信息、无法核对、缺少必要来源；
3. **操作容易误解**：不知道从哪里开始，不清楚当前状态；
4. **使用成本过高**：步骤重复、内容丢失、等待没有反馈；
5. **样式和新增功能**：不影响核心任务的美化与愿望清单。

先选择最重要的一到三个问题修改。修完以后，再请原来的用户或另一位新用户完成同一个任务，看看问题是否真的消失。

### 4.1 把观察交给 AI IDE，而不是只说“优化一下”

一条可执行的修改请求应包含：用户想完成什么、实际发生了什么、希望改成什么。比如：

```text
用户任务：上传商品图片并生成三条卖点。

观察到的问题：
两位测试者点击“生成”后再次连续点击，
因为页面没有明显显示请求已经开始，最后产生了重复任务。

请修改当前页面：
1. 请求开始后禁用生成按钮，并显示“正在生成”；
2. 请求结束或失败后恢复按钮；
3. 不要清空用户已经填写的内容；
4. 修改后告诉我怎样手动测试重复点击和失败情况。
```

这比“帮我优化体验”更容易得到范围清楚的改动，也方便你判断修复是否有效。

### 4.2 修改以后重新走一遍核心流程

修复一个问题，有时会带来新的问题。提交作品前，至少再测试下面四种情况：

- 一组信息完整的正常输入；
- 缺少一个必填字段；
- API 请求失败或超时；
- 得到结果后进行编辑、复制或重新生成。

如果产品会保存草稿，再补一次刷新页面后的测试。确认新功能正常以后，也要检查原来的核心流程没有被破坏。

## 5. 整理成可以展示的阶段作品

完成一轮真实试用和修改以后，产品才从“课堂练习”变成了一份有过程、有结果的阶段作品。展示时不需要夸大功能，清楚说明你解决了什么问题、做出了什么，以及用户反馈怎样改变了产品。

### 5.1 用一分钟讲清楚作品

可以按照下面的顺序介绍：

1. **谁遇到了什么问题**：例如电商运营准备首版图文时，要反复整理图片和卖点；
2. **产品怎样帮助他**：上传商品图片和资料，生成一版可以继续修改的内容；
3. **接入了什么 AI 能力**：图片理解与文本生成；
4. **用户怎样完成任务**：上传、生成、检查、修改、复制；
5. **试用后改了什么**：例如补充等待状态，并在失败后保留输入。

这五件事比“使用了某某框架和模型”更能说明作品的价值。技术可以作为补充，但不应该替代问题、用户和结果。

### 5.2 准备三份交付内容

第一阶段建议留下下面三项内容：

1. **可以运行的应用**：提供访问链接；如果还没有部署，则写清启动命令和访问地址；
2. **30～60 秒演示视频**：从输入开始，完整展示一次核心任务，不要只快速切换页面；
3. **一页项目说明**：写明目标用户、核心问题、主要流程、AI 能力、一次用户反馈，以及根据反馈完成的修改。

如果暂时无法让别人远程访问，本地运行加演示视频也可以作为第一阶段成果。重点是别人能够看懂你做了什么，并看到核心流程确实可以完成。

### 5.3 选择继续完善的作品

你可以继续完成贯穿本章的电商内容工作台，也可以把同一套方法用到自己的场景，例如会议整理、有声内容、学习辅助或行业小工具。[AI 应用场景参考](../appendix-industry-scenarios/index.md)可以帮助你扩展方向。

不必为了显得原创而重新做一个陌生题目。一个来自自己学习、工作或生活的问题，即使范围很小，只要经过真实试用和修改，也比功能很多但没有人用过的页面更有说服力。

### 阶段作品检查清单

- [ ] 一位新用户能够独立开始并完成核心任务；
- [ ] 至少有一种 AI 能力在真实调用，而不是预设结果；
- [ ] 页面能说明等待、成功和失败状态；
- [ ] AI 返回的内容可以检查，并能继续编辑、复制或保存；
- [ ] 已邀请至少一位真实用户试用，并记录具体行为；
- [ ] 已根据反馈完成一轮修改和重新测试；
- [ ] API Key 没有出现在前端代码、截图或提交记录中；
- [ ] 使用的图片、声音、文档和人物素材拥有必要授权；
- [ ] 已准备运行方式、演示视频和简短项目说明。

## 下一步

完成这份作品后，你已经走过一条完整路径：从真实问题出发，收敛第一版方案，做出可交互原型，接入 AI，再把产品交给用户试用和修改。

进入 Stage 2 后，我们会继续学习数据库、用户系统、支付、部署和更完整的前后端工程。它们会让产品支持更多用户和更真实的数据，但出发点仍然是这一阶段学会的事情：先把一条有价值的用户任务做完整。

<RelatedArticlesSection
  title="继续进阶"
  description="完成 Stage 1 后，可以继续学习下面的工程化内容。"
  :items="relatedArticles"
/>
